# Data Warehouse - Environment Variables Example
# Copy this file to .env and update with your values

# ==========================================
# Kafka Configuration
# ==========================================
# For running OUTSIDE Docker (development)
# KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# For running INSIDE Docker (production)
KAFKA_BOOTSTRAP_SERVERS=kafka:29092

# Kafka Topics
KAFKA_DATASET_TOPIC=dataset-events
KAFKA_BIAS_TOPIC=bias-events
KAFKA_AUTOML_TOPIC=automl-events
KAFKA_XAI_TOPIC=xai-events
KAFKA_BIAS_TRIGGER_TOPIC=bias-trigger-events
KAFKA_AUTOML_TRIGGER_TOPIC=automl-trigger-events
KAFKA_XAI_TRIGGER_TOPIC=xai-trigger-events

# Kafka Consumer Groups (use unique IDs for your service)
KAFKA_CONSUMER_GROUP=my-service-consumer
KAFKA_BIAS_CONSUMER_GROUP=my-bias-consumer
KAFKA_AUTOML_CONSUMER_GROUP=my-automl-consumer
KAFKA_XAI_CONSUMER_GROUP=my-xai-consumer

# ==========================================
# Data Warehouse API
# ==========================================
# For running OUTSIDE Docker (development)
# API_BASE=http://localhost:8000

# For running INSIDE Docker (production)
API_BASE=http://dw-api:8000

# API Authentication (if enabled)
# API_KEY=your-api-key-here

# ==========================================
# MongoDB Configuration (Direct Access)
# ==========================================
# NOT RECOMMENDED - Use API instead!
# Only use if absolutely necessary

# For running OUTSIDE Docker
# MONGODB_URL=mongodb://localhost:27017

# For running INSIDE Docker
# MONGODB_URL=mongodb://mongodb:27017

# MongoDB Database Name
# MONGODB_DATABASE=data_warehouse

# ==========================================
# MinIO Configuration (Direct Access)
# ==========================================
# NOT RECOMMENDED - Use API instead!
# Only use if absolutely necessary

# For running OUTSIDE Docker
# MINIO_ENDPOINT=localhost:9000

# For running INSIDE Docker
# MINIO_ENDPOINT=minio:9000

# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_SECURE=False
# MINIO_BUCKET_NAME=data-warehouse

# ==========================================
# Service-Specific Configuration
# ==========================================
# Add your service-specific settings here

# Logging Level
LOG_LEVEL=INFO

# Service Name
SERVICE_NAME=my-service

# Request Timeout (seconds)
REQUEST_TIMEOUT=30

